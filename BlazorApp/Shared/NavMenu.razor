<div class="top-row ps-3 navbar" style="background: lightgrey">
    <div class="container-fluid">
        <a>
            <img src="Floor234_Logo.png" alt="Floor234" width="150"/>
        </a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" style="background: silver" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" style="color: black" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true" style="color: black"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3" >
            <NavLink class="nav-link" href="Floors" style="color: black">
                <span class="oi oi-aperture" aria-hidden="true" style="color: black"></span> Floors
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
